; 图片上传相关限制（2026-09 二期图片上云时加）
; PHP 默认 upload_max_filesize=2M —— 手机拍的照片 3~5M 会被直接拒掉（表现为上传接口返回空/413），
; 所以这里放开；nginx 那边 docker/nginx/local.conf 已经是 client_max_body_size 100M，不用再动。
upload_max_filesize = 20M
post_max_size = 25M
; 图片处理（后端要读文件内容推 OSS）留点余量
memory_limit = 256M
