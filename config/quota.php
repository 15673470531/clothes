<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 免费额度（2026-09）
    |--------------------------------------------------------------------------
    |
    | 照片存在云上要花钱，所以每个人能录的衣物数量是有限的。
    | 额度是**用户表上的余额**（users.item_quota / users.daily_quota）：
    |   - 每成功录一件衣物，两个余额各减 1（删掉不穿的不会加回来）
    |   - 每天定时任务把 daily_quota 重置回下面这个值
    |   - 以后开会员/套餐：直接在后台把那两个数字改大就行，不用改代码
    |
    | 这里只放「新用户的默认值」和「每天重置成多少」。
    |
    */

    // 新用户送多少件衣物额度
    'item_quota' => env('QUOTA_ITEM', 200),

    // 每天最多录几件（定时任务每天重置成这个数）
    'daily_quota' => env('QUOTA_DAILY', 50),

];
