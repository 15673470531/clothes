<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 免费衣架额度（2026-09 · 第三版：额度 = 衣架）
    |--------------------------------------------------------------------------
    |
    | 一个衣架挂一样东西：录一件衣物占 1 个，存一套搭配也占 1 个（共用一个架子）。
    | 照片存在云上要花钱，所以免费能挂的数量有限。
    |
    | 额度是**用户表上的余额**（users.item_quota / users.daily_quota）：
    |   - 新增一件衣物/一套搭配，两个余额各减 1
    |   - 删掉衣物/搭配：**总额度加回来**（云端真少了一张图）；今日额度不回补（防"删了再加"无限循环）
    |   - 改名字/换衣物/重出封面这类"换一张"的操作不占（旧图会被删，云端总数没变）
    |   - 每天定时任务把 daily_quota 重置回下面这个值
    |   - 开会员/套餐：直接在后台把那两个数字改大就行（Filament），不用改代码
    |
    | 这里只放「新用户的默认值」和「每天重置成多少」。改数字记得同步
    | docs/衣架规则.md 和 pages/hanger 说明页的文案（说明页的数字是接口下发、不用手改）。
    |
    */

    // 新用户免费送多少（总数）
    // 2026-09 用户定稿：100。**只在建号那一刻给一次**（UserController::login 按这个数写进
    // users.item_quota），之后不会再送 —— 用户能加的只有「删除退还」和「赚衣架」那几种（奖励另见下面）。
    'item_quota' => env('QUOTA_ITEM', 100),

    // 每天最多挂几个（定时任务每天重置成这个数）
    'daily_quota' => env('QUOTA_DAILY', 100),

    /*
    |--------------------------------------------------------------------------
    | 赚衣架：每日签到 / 分享好友（2026-09 用户定）
    |--------------------------------------------------------------------------
    |
    | 「我的」页那张衣架卡上的「获取更多」进的就是这个：签到 +2、分享给好友 +10。
    |
    | 两条口径：
    |   - 奖励**加到总额**上（不动今日额度）—— 赚来的是资产
    |   - 奖励**不受免费总量上限约束**（余额可以超过 item_quota）—— 那个上限只是"免费进货量"，
    |     奖励是额外赚的；分母在接口里取 max(免费标准, 余额)，所以后台加量/奖励都跟着走
    |
    | 次数上限写在这里，改数字只改配置（.env 可覆盖），不用发小程序版本。
    |
    */

    // 每日签到送几个
    'reward_checkin' => env('QUOTA_REWARD_CHECKIN', 2),

    // 分享给好友送几个
    'reward_share' => env('QUOTA_REWARD_SHARE', 10),

    // 每种奖励每天最多领几次
    'reward_checkin_daily' => env('QUOTA_REWARD_CHECKIN_DAILY', 1),
    'reward_share_daily'   => env('QUOTA_REWARD_SHARE_DAILY', 1),

    // 新用户每月免费领取多少（2026-09 用户改：把"每月系统赠送"改成"点一下领取"）
    //
    // 跟原来「每月系统赠送」的区别：**周期还是每月 1 次，但从"系统自动到账"改成"用户点一下领取"**
    // （入口在「赚衣架」页原来那一行的位置，标题「新用户每月免费领取」）。
    // 口径：
    //   - 按月算（自然月），一个月能领 1 次；领了记一条流水（type=newcomer，reward_date=领取当天）
    //   - **不点就没有** —— 这个月没点，下个月还能领（不补发、不累计）
    //   - 奖励跟签到/分享一样：只加总额、不受免费上限约束
    // 设成 0 = 关掉这一行（接口不给标题，小程序就不显示）
    'reward_newcomer' => env('QUOTA_REWARD_NEWCOMER', 50),

    // 每月系统免费赠送多少（2026-09 上线过一版：按月自动到账、不用点）
    // **已被上面「新用户每月免费领取」取代（2026-09 用户改）**，默认关掉。
    // 留着这个开关不是为了重新上线，而是万一想退回"自动发"：把它设成 >0，
    // 惰性补发那条路径（HangerRewardService::grantMonthly）还在，见 Service 里的说明。
    'reward_monthly' => env('QUOTA_REWARD_MONTHLY', 0),

];
